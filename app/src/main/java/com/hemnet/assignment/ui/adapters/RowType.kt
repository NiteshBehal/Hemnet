package com.hemnet.assignment.ui.adapters

/**
 * enum class to identify row types for property list
 * */
enum class RowType(val type: String) {
    AREA("Area"),
    PROPERTY("Property"),
    HIGHLIGHTED_PROPERTY("HighlightedProperty");
}